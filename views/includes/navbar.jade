.navbar.navbar-inverse.navbar-fixed-top
  .navbar-inner
    .container
      a.btn.btn-navbar(data-toggle="collapse", data-target=".nav-collapse")
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.brand(href="/") Microblog
      .nav-collapse
        ul.nav
          li.active: a(href="/") 首页
          - if (!user)
            li: a(href="/login") 登入
            li: a(href="/reg") 注册
          - else 
            li: a(href="/logout") 登出
        ul.nav.pull-right
          if user
            li: a(href="/u/#{user.name}") #{user.name}
